import React, { Component } from 'react';

const metadata = {

};

const crumbs = [
  { to: '/', label: 'nickclaw' },
  { to: '/work', label: 'work' }
];

export function Work(props) {

  return (
    <section className={`container`}>
      
    </section>
  );
}
